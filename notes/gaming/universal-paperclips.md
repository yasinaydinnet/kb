# Universal Paperclips

Game URL: <https://www.decisionproblem.com/paperclips/>

## Cheat (in browser console)

Part 1:

```js
funds = 10000000000000;
margin=0.02; lowerPrice();
marketingLvl=500;marketingLvlElement.innerHTML = marketingLvl;
clipmakerLevel=10000;clipmakerLevel2Element.innerHTML = clipmakerLevel;
megaClipperLevel=100000000;megaClipperLevelElement.innerHTML = megaClipperLevel;
processors=99999;addProc();
memory=9999;addMem();
yomi = 1000000;yomiDisplayElement.innerHTML = formatWithCommas(yomi);
```

Part 2:

```js
[...Array(100)].forEach(()=>{ buyWire() });
[...Array(10000)].forEach(()=>{ clipClick(1) });
makeClipper();clipmakerLevel=10000;clipmakerLevel2Element.innerHTML = clipmakerLevel;
```

Part 3: /Click all projects, except XAVIER REINITIALIZATION !

Part 4:

```js
factoryLevel=10000;factoryLevelDisplayElement.innerHTML = factoryLevel;fcmod = 1.10;updateUpgrades();
farmLevel=1000000;farmLevelElement.innerHTML = formatWithCommas(farmLevel);updatePowPrices();
wireDroneLevel=10000000;makeWireDrone(0);
harvesterLevel=10000000;makeHarvester(0);
batteryLevel=10000;makeBattery(0);
```

Part 5:

```js
probeTrust=10000;probeTrustDisplayElement.innerHTML = probeTrust;
[...Array(1000)].forEach(()=>{ raiseProbeSpeed() });
[...Array(1000)].forEach(()=>{ raiseProbeNav() });
[...Array(1000)].forEach(()=>{ raiseProbeRep() });
[...Array(1000)].forEach(()=>{ raiseProbeHaz() });
[...Array(1000)].forEach(()=>{ raiseProbeFac() });
[...Array(1000)].forEach(()=>{ raiseProbeHarv() });
[...Array(1000)].forEach(()=>{ raiseProbeWire() });
[...Array(1000)].forEach(()=>{ raiseProbeCombat() });
probeDriftBaseRate=0.00000001;
```

Part 6: Finishing the game

```js
[...Array(95)].forEach(()=>{ clipClick(1) });
```
